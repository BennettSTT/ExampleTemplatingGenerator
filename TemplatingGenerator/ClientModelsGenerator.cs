using System.Text;
using Microsoft.VisualStudio.TextTemplating;

namespace TemplatingGenerator
{
    public class ClientModelsGenerator
    {
        public void Initialize(ITextTemplatingEngineHost host, StringBuilder template)
        {
            // initialization of fields
        }

        public virtual void Generate()
        {
            // code generation logic
        }
    }
}